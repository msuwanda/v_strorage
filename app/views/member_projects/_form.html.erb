<%= form_for(@member_project) do |f| %>
  <% if @member_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_project.errors.count, "error") %> prohibited this member_project from being saved:</h2>

      <ul>
      <% @member_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.number_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
